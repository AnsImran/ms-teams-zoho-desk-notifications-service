[auth]
username = "admin"
name = "Admin"
password_hash = "$2b$12$..."    # Generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
cookie_key = "change-this-to-a-random-string"
