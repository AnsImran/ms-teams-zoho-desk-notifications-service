Please do a formatting-only pass on these files to match the style used in `main.py`:

- `src/scripts/critical_findings_watch.py`
- `src/scripts/superstat_watch.py`
- `src/scripts/code_stroke_watch.py`
- `src/scripts/pending_watch.py`
- `src/core/watch_helper.py`

Rules:
1) Do NOT change logic, behavior, variable names, function names, strings, imports, or execution order.
2) Keep grouping of similar lines consistent with `main.py`.
3) Normalize spacing/blank lines/indentation for readability.
4) In each similar contiguous assignment block, align the first `=` across lines.
5) After aligning `=`, re-align inline comments in that same block.
6) Remove trailing whitespace.
7) Keep the edit whitespace/alignment-only (`git diff -w` should show no semantic changes).
8) For multi-line function parameter blocks that include type annotations and default values, align the annotation/value columns for readability (for example, align types and then align `=` in that contiguous parameter block).
9) Apply these formatting rules consistently across the entire target file wherever a similar pattern appears, not only in the specifically highlighted snippet.
10) In multi-line dictionary blocks, align key/value columns consistently (and keep comma placement visually consistent) within that contiguous block.
11) In repeated single-line function-call blocks with positional arguments, align argument columns by spacing after each comma (keep commas attached to the preceding token).
12) After aligning call-argument columns, align inline comments consistently across that contiguous block when comments are present.

After formatting, run a quick syntax check and report completion.

---

run all tests:
uv run --with pytest pytest -q

uv run --with pytest pytest tests/core/test_watch_helper_search_tickets.py -q

